LolRSS is a simple feed agregator written in perl for fun's sake.

to do list:
   -minor error checking,
   -refactor code to be more efficient and fit the 80*50 rule,
   -get rid of annoying sigil ruinning the display (utf8 ?);
 
